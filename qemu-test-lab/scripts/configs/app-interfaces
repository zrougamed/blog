# /etc/network/interfaces - Application VM
# 
# This configuration is for Alpine Linux
# Copy to /etc/network/interfaces inside the app VM
#
# Network setup:
# - eth0: Internal network (10.0.100.10/24)
# - Gateway: 10.0.100.1 (gateway VM)

auto lo
iface lo inet loopback

# Internal network interface
auto eth0
iface eth0 inet static
    address 10.0.100.10
    netmask 255.255.255.0
    gateway 10.0.100.1

# DNS configuration
# Either use gateway as DNS (if configured) or public DNS
# Add to /etc/resolv.conf:
#   nameserver 8.8.8.8
#   nameserver 8.8.4.4

# After configuration:
#
# 1. Apply network settings:
#    rc-service networking restart
#
# 2. Test connectivity:
#    ping 10.0.100.1          # Gateway
#    ping 10.0.100.20         # Attacker VM
#    ping 8.8.8.8             # Internet (through gateway)
#
# 3. Configure hostname:
#    echo "app" > /etc/hostname
#    hostname -F /etc/hostname
#
# 4. Install application packages:
#    apk update
#    apk add docker docker-compose  # Example: Docker
#    rc-update add docker
#    rc-service docker start
